# Optional: point to your own cobalt instance.
# Default: https://dwnld.nichind.dev (public community instance, no auth required)
# Note: https://api.cobalt.tools requires JWT/API-Key authentication.
COBALT_API_URL=
